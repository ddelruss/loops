  <%= form_for @current_login, :url => {:action => "nodes"}, :method => "get" do |f| %>
    Google User ID:
    <%= f.text_field :user %>
    <p>
    Password (not saved!):  
    <%= f.password_field :password %>
    <p>
    Document key:  
    <%= f.text_field :key %>
    <p>
      <%= f.submit "Nodes" %>
      <%= f.submit "Connections" %>
      <%= f.submit "Diagram 1" %>
  <% end %>
  
