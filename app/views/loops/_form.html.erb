  <%= form_for @current_login, :url => {:action => "nodes"}, :method => "post" do |f| %>
    Google User ID:
    <%= f.text_field :user %>
    <p>
    Password (not saved!):  
    <%= f.password_field :password %>
    <p>
    Document key:  
    <%= f.text_field :key %>
    <p>
      <%= f.submit "Hierarchy" %>
      <%= f.submit "Relationships" %>
      <%= f.submit "Projects" %>
      <%= f.submit "Relationship Circle" %>
  <% end %>
  
